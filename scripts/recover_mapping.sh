#/bin/bash
PROJECT_DIRECTORY_PATH="${1}"
BACKUP_TIMESTAMP="${2}"
BACKUP_DIR_PATH="${PROJECT_DIRECTORY_PATH}/backup-${BACKUP_TIMESTAMP}"

rsync -arziv "${BACKUP_DIR_PATH}/" "${PROJECT_DIRECTORY_PATH}"
echo ${BACKUP_TIMESTAMP}
